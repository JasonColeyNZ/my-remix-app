import React from "react";
import { Card, CardContent } from "~/components/ui/card";

const DBClients = () => {
  return (
    <Card>
      <CardContent>Number of Clients</CardContent>
    </Card>
  );
};

export default DBClients;
