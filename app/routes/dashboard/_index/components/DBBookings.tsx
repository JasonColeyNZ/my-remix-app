import React from "react";
import { Card, CardContent } from "~/components/ui/card";

const DBBookings = () => {
  return (
    <Card>
      <CardContent>Number of Bookings</CardContent>
    </Card>
  );
};

export default DBBookings;
