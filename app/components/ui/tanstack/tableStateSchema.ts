// import { z } from "zod";
// import { zx } from "zodix";

// export const tableStateSchema = z.object({
//   table: z.string(),
//   showColumnFilters: zx.BoolAsString,
//   sorting: z.string().transform((obj) => JSON.parse(obj)),

//   columnFiltering: z.string().transform((obj) => JSON.parse(obj)),
// });
